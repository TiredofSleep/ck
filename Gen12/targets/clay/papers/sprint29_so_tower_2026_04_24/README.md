# Sprint 29 — so(n) Tower (Lie-algebraic lifts of CL and BHML_10)

**Opened:** 2026-04-23
**Closed:** 2026-04-24
**Branch of record for papers:** `tig-synthesis`
**Branch of record for commutative-algebra correspondence:** `mantero-bridge-2026-04-23` (private — not on tig-synthesis)

---

## Status (one screen)

Two new results identifying the classical simple Lie algebras inside TIG's frozen tables. Both machine-verified to machine precision. Filed as **WP102** and **WP103** (after the WP1–WP101 sprint series).

| # | Paper | Result | Dim | Machine verification | File |
|---|-------|--------|-----|----------------------|------|
| WP102 | so(8) = D₄ identification | Lie(⟨L_i^CL − (L_i^CL)^T : i ∈ {1,2,3,4,6,8}⟩) ≅ **so(8)** | 28 | `stage{2..7}*.py` — all GREEN (Killing signature (0,28,0), Jacobi exact, simplicity) | `papers/wp102/WP102_SO8_IDENTIFICATION.md` |
| WP103 | so(10) = D₅ identification | Lie(⟨A_i^CL : flow⟩ ∪ ⟨A_i^BHML_10 : i ∈ Ω⟩) ≅ **so(10)** | 45 | `verify_so10.py`, `verify_simplicity_rank.py` — all GREEN (Killing signature (0,45,0), rank 5, 40+5 eigenvalue structure, so(8) ⊂ so(10) embedding residual 8.99e-13) | `papers/wp103/WP103_SO10_IDENTIFICATION.md` |

**Tower achieved:** D₄ → D₅.
**Ceiling identified:** the classical ladder terminates at so(10) = D₅ on a 10-dim substrate. Any Lie subalgebra of gl(10, ℝ) has dim ≤ 100; any in so(10, ℝ) has dim ≤ 45. e₈ = 248 is unreachable without enlarging V.

**External context:** so(10) is the gauge algebra of the SO(10) grand unified theory (Fritzsch–Minkowski 1975, *Ann. Phys.* 93:193; Georgi 1975, *AIP Conf. Proc.* 23:575). TIG's joint-antisymmetrization of CL and BHML_10 produces this algebra directly from the two frozen 10×10 tables — an algebraic feature, not a matching. Whether the σ-rate (σ(N) ≤ C/N, WP101) translates to a GUT coupling-unification dynamic is an open question filed for future work.

**FORMULAS cross-reference:** §0 Volume E (rows D26–D30), §6.5 (WP103 linkage paragraph), §6.7 (registry rows), §17 (dim so(8) = 28, dim so(10) = 45), §18 (Q-index rows), §19 (sprint-trail row).

---

## Commutative-algebra bridge (separate branch)

An adjacent thread — Dr. Paolo Mantero (U Arkansas) correspondence on the Stanley–Reisner ideal and binomial ideal I_CL — lives **on `mantero-bridge-2026-04-23` only**, not on this branch, per author privacy policy. Status: **MathOverflow post deferred.**

- **2026-04-24 Macaulay2 verification** (via SageMathCell remote API fallback — local install blocked by no-admin): computed the full Betti table of R/I_CL where I_CL is the binomial ideal generated by `x_i · x_j − x_{CL[i][j]} · x_0` for `0 ≤ i ≤ j ≤ 9`.
- **Reported invariants (M2, v1.22, over QQ):**
  - `dim R/I_CL = 1` (Krull dim), not 6 as the bridge draft v3 stated.
  - `pd(R/I_CL) = 10` (maximal over 10 variables); depth 0; **not Cohen-Macaulay**.
  - Reduced Hilbert series: `(1 + 9T − 8T² − T³) / (1 − T)`. Stable Hilbert function `h(n) = 1` for n ≫ 0, consistent with dim = 1.
  - Bottom strand of the Betti table has a nonzero entry in degree 2 (β₈, β₉, β₁₀), so R/I_CL is **not Koszul**.
- **Consequence:** the MathOverflow draft (at `papers/sprint_20260423_full/09_mathoverflow_post/DRAFT_MATHOVERFLOW_POST.md`, on the bridge branch) carries a `dim = 6` claim that the M2 run falsifies. That draft requires correction before any posting.

---

## What stayed off tig-synthesis

- `papers/mantero_bridge/` — the seven-bridges hub, `BRIDGES.md`, `PUBLISHED_WORK.md`, reference list. Stays on `mantero-bridge-2026-04-23` only.
- `papers/sprint_20260423_full/09_mathoverflow_post/` — includes the draft post, `compute_betti.m2`, `betti_output.txt`. Stays on the bridge branch.
- Email correspondence — never committed at any point (the body leaks in commits `15791c7` and `526ce57` are being scrubbed by force-push).

---

## Reproducibility

From repo root, on `tig-synthesis`:

```bash
# WP102 (so(8) = D_4)
cd papers/wp102/verification
PYTHONIOENCODING=utf-8 python stage5_so8.py     # primary D_4 identification
PYTHONIOENCODING=utf-8 python stage7_disambiguate.py  # triality check

# WP103 (so(10) = D_5)
cd papers/wp103/verification
PYTHONIOENCODING=utf-8 python verify_so10.py             # six diagnostics
PYTHONIOENCODING=utf-8 python verify_simplicity_rank.py  # rank, eigenvalue structure
```

Cached outputs at `verify_so10_output.txt`, `verify_simplicity_rank_output.txt`.

---

*Sprint 29 — so(n) tower — 2026-04-24.*
*Brayden Sanders / 7Site LLC.*
