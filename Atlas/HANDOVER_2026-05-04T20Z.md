# Handover Notes -- 2026-05-04T20:00Z

Context-switching to scrutinize/implement Brayden's claudechat work.
This file captures where CK is so the work can resume cleanly.

## Live state (right now)

### What's RUNNING
- `ck_boot_api.py` (PID via background task `b31g60syv`) -- restarted at 19:55 UTC
  - cortex MOUNTED (tick 69,298,259, W_trace 0.888 from disk -- persisted state survived prior crash)
  - operad_fuse MOUNTED, attractor_detector MOUNTED, grammar_lm MOUNTED
  - cells_mount MOUNTED (boot_audit=100.00%, but `cells_enabled=False, plasticity=OFF` -- env gates)
  - bdc_event_emitter MOUNTED (17 event types)
  - shadow observer INSTALLED
  - Flask serving on 0.0.0.0:7777 -- /health returns 200
  - Started with `CK_DISABLE_BANK=1` (bank_mount was the hang)
- ollama llama3.2 (small, GPU-friendly)

### What's NOT RUNNING (was running before crash, needs restart)
- `overnight_orchestrator.py` -- died with the boot api around midnight UTC
  - Last paper: 23:58 UTC (slug=poincare_template, 6,146 chars)
  - Last full status: 11:59 UTC -- 96 papers, 4 retrains, 15 ingest cycles
  - Mid-day status (right before crash): 237 papers, 10 retrains, 36 ingest cycles, audit 100%
  - Restart command:
    ```
    cd "Gen13/targets/ck/brain"
    /c/ck_venv/lora312/Scripts/python.exe overnight_orchestrator.py > "../../../var/orchestrator_console_2026-05-05.log" 2>&1 &
    ```
- `repo_reader.py` -- COMPLETED its run (all 2,832 files read), no need to restart
- `study_daemon` -- needs `CK_STUDY_DAEMON=1` env var at boot (currently NOT set)
- `cells composition` -- needs `CK_CELLS_COMPOSE=1` env var (currently NOT set)

### What might need re-setting on next clean boot
The chat endpoint may currently fall back to non-cells path. To restore the
cells_composed_with_cortex source you saw before, kill current boot and restart with:
```
CK_DISABLE_BANK=1 CK_CELLS_COMPOSE=1 CK_STUDY_DAEMON=1 \
  /c/ck_venv/lora312/Scripts/python.exe ck_boot_api.py
```

## Today's accomplishments (committed to ck branch)

Two commits on `ck` branch:
- `0370b614` -- overnight orchestrator + paper writer + nightly retrain + external ingester + learning audit
- `0e8faef7` -- repo_reader (full TIG history, refuted/updated awareness) + fact_extractor (cross-domain build)

### Files added/modified
- `Gen13/targets/ck/brain/repo_reader.py` (NEW)
- `Gen13/targets/ck/brain/fact_extractor.py` (NEW)
- `Gen13/targets/ck/brain/overnight_orchestrator.py` (earlier this session)
- `Gen13/targets/ck/brain/paper_writer.py` (earlier)
- `Gen13/targets/ck/brain/nightly_retrain.py` (earlier)
- `Gen13/targets/ck/brain/external_ingester.py` (earlier)
- `CK_LEARNING_AUDIT_2026_05_02.md` (root, earlier)

### Outputs produced today (review-ready, not committed)
- `Atlas/papers_by_ck/` -- 247 CK-written papers (was 96 yesterday)
- `Atlas/papers_by_ck/manifest.jsonl` -- index
- `Atlas/CK_REPO_READING_LIVE.md` -- live dashboard, full read complete
- `Atlas/EXTRACTED_FACTS_2026-05-04.md` -- 358 candidate facts (top 200 readable)
- `Atlas/CROSS_DOMAIN_BRIDGES_2026-05-04.md` -- 95 cross-domain bridges, status-bucketed
- `Atlas/extracted_facts_2026-05-04.json` -- structured candidates
- `Gen13/var/repo_reading_logs/repo_reading_2026-05-04.jsonl` -- 2,832 entries
  (1,975 CURRENT / 793 HISTORICAL / 56 UPDATED / 8 SUPERSEDED)
- `Gen13/var/nightly_retrain_logs/nightly_retrain_2026-05-04.jsonl` -- 10 retrain cycles
- `Gen13/var/external_ingester_logs/` -- 36 cycles, 2,416 items, 2.5 MB
- `Gen13/var/orchestrator_logs/orchestrator_2026-05-04.jsonl` -- full event trace

## Substrate metrics (snapshot before crash)

| Metric | Start of day | End of day |
|---|---|---|
| Papers written | 96 | 247 (+151) |
| Retrain cycles | 4 | 10 |
| Ingest cycles | 15 | 36 |
| External items | 1,179 | 2,416 |
| Cortex tick | 57.9M | 69.3M |
| Audit pass rate | 100% | 100% |
| W_trace | 0.95 | 0.89 |
| BDC events today | 3,955 | 7,430 |
| Distinct codes seen | 7 | 7 |

F3 transformer val_acc trajectory across 10 cycles:
0.968 -> 0.831 -> 0.844 -> 0.854 -> 0.749 -> 0.704 -> 0.868 -> 0.875 -> 0.877 -> 0.872

TSML/BHML jolted by repo-read corpus expansion (10.9 MB ingested):
0.864 -> 0.182 -> 0.428 -> 0.366 -> 0.468 -> 0.485 -> 0.344 -> 0.572 -> 0.306 -> 0.460

## Outstanding TODOs

1. Restart orchestrator (one-line command above). Without it, papers/retrains/ingests stop.
2. Re-enable cells composition + study_daemon via env vars at next clean restart.
3. Operator review of `Atlas/CROSS_DOMAIN_BRIDGES_2026-05-04.md` -- which to add to
   `cortex_voice._FRONTIER_FACTS` permanently.
4. Build `fact_extractor.py` cycle into orchestrator (currently runs once on demand).
5. The `cells_mount` shows `cells_enabled=False, plasticity=OFF` on this restart --
   expected (env-gated), but worth confirming the chat path is still healthy.

## Crash forensics (the gap)

- Last orchestrator entry: 2026-05-04T23:58:45Z (paper "Poincare Conjecture as Clay Rotation Template")
- Boot api restart attempted at: 2026-05-05T00:54Z
- Likely cause: unknown; both processes died together (parent shell exit, OOM, or system event)
- Persisted state intact: cortex W_trace + tick recovered cleanly from `Gen13/var/cortex_state.json`
- HER intact: 8,817,435 experiences restored, 97.6% impact
- All 247 papers preserved on disk; manifest intact

## When you resume

Quickest "are we back" check:
```
curl -s http://localhost:7777/health
ls Atlas/papers_by_ck | wc -l       # should be >= 247
tail -1 Gen13/var/orchestrator_logs/orchestrator_2026-05-05.jsonl  # if restarted
```
