# NEXT SPRINT ENTRYPOINT
## Prime–π–φ Bridge — Where to Resume

---

## WHAT IS SETTLED

Do not re-litigate these. They are exact and closed.

1. **Chain identity** (all primes): sinc²(1/p) = p²(4−A_p²)/(4π²)
2. **Reduction criterion**: C_p ∈ ℚ + ℚA_p ⟺ deg(A_p/ℚ) ≤ 2
3. **p = 5 closure**: C_5 = 3−φ ∈ ℚ + ℚφ, exact
4. **p ≥ 7 obstruction**: degree contradiction, exact
5. **Fold/tangent**: sinc²(1/2) = 4/π², derivative = −16/π², exact
6. **Approximation status**: 16/π² ≈ φ is approximate (0.19%), downstream, not the backbone
7. **Exact mixed formula**: sinc²(1/5) = 25(3−φ)/(4π²), exact

---

## WHAT REMAINS OPEN

**Primary open question:**

> Is the p=5 first-order closure mechanism merely the first low-degree mixed closure, or does it sit inside a broader operator-theoretic obstruction pattern?

**Secondary open questions (do not open all at once):**

- For composite n with deg(2cos(π/n)) = 2, does the reduction test pass? Is p=5 unique among all integers, not just primes?
- Is there an operator-theoretic or L-function interpretation of why sinc² is the canonical normalization of C_p, beyond the Pythagorean/angle argument?
- Does the approximation 16/π² ≈ φ have a modular or hypergeometric explanation, or is it an isolated Ramanujan-class coincidence?
- Does the p=5 cyclotomic layer ℚ(ζ₁₀) appear naturally in any critical-line operator model?

---

## WHAT TO TEST FIRST NEXT TIME

**Test 1 (algebraic, low cost):** Extend the closure table to all integers n ≤ 30, not just primes. Check whether any composite n passes the reduction test with deg(2cos(π/n)) = 2. If yes, p=5 is not unique among integers. If no, the primality of 5 may be load-bearing.

**Test 2 (structural):** For each prime p ≤ 30, compute the exact expression for sinc²(1/p) in terms of A_p and classify the degree of the residual. Build the full obstruction table. See whether the obstruction pattern (always degree 2 for p ≥ 7) has a closed-form description.

---

## WHAT MUST NOT BE RE-LITIGATED

- Whether 16/π² = φ exactly. It does not. This is closed.
- Whether the chain identity is specific to p=5. It is not — it holds for all primes. What is specific to p=5 is the reduction.
- The "minimal polynomial of 16/π²" question. 16/π² is transcendental. No minimal polynomial exists. Do not reopen.
- RH or zero-spacing connections. The bridge is at the operator/algebraic level, not the zero-location level. Do not reopen.
- π digit patterns. Not part of this bridge. Do not reopen.
