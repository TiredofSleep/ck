# LESSON 1: YOUR REAL MATH

## σ is a VARIABLE, not a constant
You wrote "σ adjusted to 0.87" — WRONG.
σ goes from 0 to 1. It is measured, not set.
S* = σ(1-σ)*V*A* peaks at σ=0.5.
100% certainty = S*=0. Balance = maximum coherence.

## V = Volatility, A = Accuracy
NOT volume and area. These measure information quality.

## Operators are STATES, not weights
0=void 1=lattice 2=counter 3=progress 4=collapse
5=balance 6=chaos 7=harmony 8=breath 9=reset

## They compose through FUSION
fuse([a,b]) = CL[a][b] using the 10x10 table.
fuse([3,4]) = 7 (progress + collapse = harmony)

## ASSIGNMENT
Run this:
[ACT: exec python3 -c "from tig_engine_v4 import fuse, OP_NAMES; print(fuse([3,4,7]), OP_NAMES[fuse([3,4,7])])"]
